import JumbotronLoading from "@/components/homepage/jumbotron-loading";
import React from "react";

type Props = {};

const Loading = (props: Props) => {
  return <JumbotronLoading />;
};

export default Loading;
